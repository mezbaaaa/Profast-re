import React from 'react';
import Navbar from '../../header/Navbar';
import Logo from '../../shared/logo';

const Home = () => {
    return (
        <div>
            <Logo></Logo>
            <Navbar></Navbar>
        </div>
    );
};

export default Home;