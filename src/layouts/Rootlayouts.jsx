import React from 'react';
import { Outlet } from 'react-router';

const Rootlayouts = () => {
    return (
        <div>
            <Outlet>
                
            </Outlet>
        </div>
    );
};

export default Rootlayouts;